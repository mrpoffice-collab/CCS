import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CCS - Grow Your Newsletter Without Meta Ads",
  description: "Acquire subscribers through Twitter, LinkedIn, SEO, and cross-promotion. All managed from one dashboard with AI-powered optimization.",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
